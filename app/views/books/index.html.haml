%h1 List of books

= link_to 'Сreate new book', [:new, :book]
- @books.each_with_index do |book, index|
  %p
    = index+1
    = book.title
    = link_to 'show', [book]
    = link_to 'edit', [:edit, book]
    = link_to 'delete', book_path(book), :confirm => 'Sure?', :method => :delete